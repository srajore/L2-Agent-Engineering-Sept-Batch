[project]
name = "l2-agent-engineering-sept-batch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
l2-agent-engineering-sept-batch = "l2_agent_engineering_sept_batch:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
