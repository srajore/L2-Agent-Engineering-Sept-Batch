[project]
name = "l2-agent-engineering-course"
version = "1.0.0"
description = "LangGraph examples for the L2 AI Agent Engineering course"
requires-python = ">=3.12,<3.14"
dependencies = [
    "langgraph>=1.0,<2.0",
    "ollama>=0.6,<1.0",
]

[tool.uv]
package = false
